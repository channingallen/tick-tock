{{input class="entry-fields__input entry-fields__time-input"
        enter=(action attrs.enter)
        focusOnInsert=true
        focus-out=(action "handleValueTranslation")
        placeholder="TIME"
        value=rawInputValue}}
